@page "/Popover"

<BxPopover IsOpen="@_isOpen" Title="Available storage" Details="This server has 150 GB of block storage remaining." Align="BxPopoverAlign.Right" HighContrast>
    <BxButton Content="点击打开/关闭 Popover" OnClick="@(() => _isOpen = !_isOpen)"/>
</BxPopover>

@code{
    bool _isOpen;
}