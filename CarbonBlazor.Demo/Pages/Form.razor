@page "/form"
@using CarbonBlazor.Demo.Models
@using Newtonsoft.Json

<BxForm @ref="form" TModel="Person" Auto Model="@person">
    @*    <BxStack Scale="7">
        <BxTextInput TValue="string" HelperText="Optional helper text here; if message is more than one line text should wrap (~100 character count maximum)" LabelText="Text input label"/>
        <BxNumberInput TValue="int" LabelText="Number input label" HelperText="Optional helper text here; if message is more than one line text should wrap (~100 character count maximum)"/>
        <BxSelect LabelText="Select an option">
        <BxSelectOption Key="Option 1"/>
        <BxSelectOption Key="Option 2"/>
        <BxSelectOption Key="Option 3"/>
        <BxSelectOption Key="Option 4"/>
        <BxSelectOption Key="Option 5"/>
        <BxSelectOption Key="Option 6"/>
        </BxSelect>
        <BxButton Kind="BxButtonKind.Primary" Content="Submit"/>
        </BxStack>*@
    <BxStack Scale="7">
        <BxButton Content="提交" OnClick="@(() => {Console.WriteLine(form?.Validate()); Console.WriteLine(JsonConvert.SerializeObject(form.Model)); })" />
    </BxStack>
</BxForm>

@code {
    BxForm<Person> form;
    Person person = new Person();
}